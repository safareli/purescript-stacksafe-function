{
  "private": true,
  "scripts": {
    "clean": "rimraf output && rimraf .pulp-cache",
    "build": "eslint src && pulp build -- --strict",
    "test": "pulp test"
  },
  "devDependencies": {
    "eslint": "^4.17.0",
    "pulp": "^12.0.0",
    "purescript": "^0.11.7",
    "purescript-psa": "^0.6.0",
    "rimraf": "^2.6.1"
  }
}
