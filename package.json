{
  "private": true,
  "scripts": {
    "clean": "rimraf output && rimraf .pulp-cache",
    "build": "eslint src && pulp build -- --strict",
    "test": "pulp test"
  },
  "devDependencies": {
    "eslint": "^5.11.1",
    "pulp": "^12.3.1",
    "purescript": "^0.12.1",
    "purescript-psa": "^0.7.3",
    "rimraf": "^2.6.2"
  }
}
